galaxy_manage_clone: yes
galaxy_manage_download: no

galaxy_server_dir: /galaxy/server

galaxy_layout: legacy-improved

galaxy_config_style: yaml
#galaxy_download_url: "https://github.com/galaxyproject/galaxy/archive/release_18.09.tar.gz"

#keep this for now, then update to 19.01 or dev
galaxy_commit_id: release_18.09

galaxy_build_client: yes 
galax_client_make_targer: client-production

pip_extra_args: "--no-cache-dir --compile"

galaxy_config:
  uwsgi:
    virtualenv: "{{ galaxy_server_dir }}/.venv"
    pythonpath: "{{ galaxy_server_dir }}/lib"
    module: galaxy.webapps.galaxy.buildapp:uwsgi_app()
    http: 0.0.0.0:8080
    static-map:
      - /static/style={{ galaxy_server_dir }}/static/style/blue
      - /static={{ galaxy_server_dir }}/static
    thunder-lock: true
    buffer-size: 16384
    offload-threads: 2
    processes: 1
    threads: 4
    die-on-term: true
    master: true
    hook-master-start:
      - unix_signal:2 gracefully_kill_them_all
      - unix_signal:15 gracefully_kill_them_all
    enable-threads: true
    py-call-osafterfork: true
  galaxy:
    database_connection: postgresql://postgres@gpsql/galaxy
 
